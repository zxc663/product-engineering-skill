# statechart-gate C7 契约对账 · 真载体电池（2026-09-24 04:18）

语料：NR7 真会话 `task-list-statechart.json`（managing/pendingUndo）+ `nr7-tasklist-contract.json`（忠实转写其恢复矩阵三行；运行时级降级按 C7 语义不录入态机契约，见 provenance）。

| 格 | 内容 | 期望 | 实测 |
|---|---|---|---|
| 正例 | 契约↔态机双向一致 | 0 | 0 ✅（含 C7 契约对账） |
| m1 | 契约抽走 UNDO 行 | 1（C7 反向：错误态出边未登记） | 1 ✅ 精确命中 |
| m2 | 契约发明 PURGE_ALL 转换 | 1（正向缺边/错误态无出路族） | 1 ✅ |

## 意义

C7（契约 recovery 双向对账）此前仅有试验仓语料与四变异证据；本电池补上**真实会话工件的契约对账正例**——NR7 的恢复矩阵三行（撤销/超窗/连删替换）与其 statechart 逐边对得上，说明真会话在档3 下自发保持了「承诺↔态机」的一致性（这正是候选 12 要防的「承诺落空/未登记发明」）。

诚实边界：契约由主会话判定轮从 NR7 spec 忠实转写（转写方法先例同 product-object-gate）；「会话原生产出契约 JSON 并自跑 C7」仍是更高一档的待验证项（轻量档/当前判例会话未到该深度）。
