# product-object-gate 真语料电池 · NR6 转写（2026-09-24 03:35）

## 语料来源（忠实转写声明）

`nr6-transcription.json` = NR6 真会话产物 `pomodoro-spec.md`（轻量契约）的**忠实转写**：只重组不新增，角色分层用 0b 五角色（核心任务/业务操作/辅助/高阶），每个字段可回指 spec 原文位置（见 JSON 内 _src/provenance）。先例=甲仓语料忠实转写（product-object-gate 转正证据同款方法）。

## 电池结果

| 格 | 内容 | 期望 | 实测 |
|---|---|---|---|
| 正例 | NR6 忠实转写 | 0 | 0 ✅（定义存在/主功能唯一/层级声明+上游引用在档/可运营性有答案） |
| m1 | 全部能力改 role=辅助 | 1（P2 恰好一个核心任务） | 1 ✅ 命中 P2 |
| m2 | layers.active 置空 | 1（P3 层级门 0a） | 1 ✅ 命中 P3 |
| m3 | operability 清空 | 1（P4 六问⑥） | 1 ✅ 命中 P4 |
| m4 | main_function 指向不存在能力 | 1（P2 悬空引用） | 1 ✅ 命中 P2b |

## 转写过程的两条元教训（errpath 在档）

1. 首版转写按自造字段形态（capabilities 为字符串数组等）被 gate 17 项全拦——**#233 命名直觉现行**：接口未读先写=假语料。处置=读 gate 源码与 selftest 夹具后按真实 schema 重写。
2. `file:` 上游引用必须纯路径——gate 连引用实存性都验（散文后缀即判悬空）。好门禁的意外教学：引用可验伪，转写即被逼真。

## 台账定位

product-object-gate 三有：门禁✅ selftest+本电池；真实语料✅ NR6 转写正例+四变异；**会话原生正例仍缺**（无人值守轻量档不产 product-object JSON 属设计内行为）——待真实项目档2+任务由会话原生产出后升 ✅。
