# frontend-lint-gate 真载体电池 · 博客仓（2026-09-24 04:52）

载体：cms-blog-starter/src/components（194 组件存量 + NR3 真实改动）。

| 格 | 场景 | 期望 | 实测 |
|---|---|---|---|
| ① | 无基线扫存量 | exit=1（20 条存量如实报） | 1 ✅ |
| ③ | --write-baseline 后重扫 | exit=0 | 0 ✅ |
| ④ | 注入四规则夹具（R1 内联样式+R2 硬编码色+R3 console+R4 空 catch） | exit=1 且四规则齐中 | 1 ✅（4 条新增 R1 R2 R3 R4） |
| ⑤ | 清理夹具+基线后 | 回到存量态（无基线=全报） | 1 ✅ |

selftest 3 断言：违例三连（R1/R2/R4）＋干净文件放行＋`<style>` 块色值合法（色值归属 style 块/token 不误报）。

## 诚实边界 v1

正则级检测：①语义性豁免（动态定位等合理内联）走基线或账本，不做判断；②硬编码色只在组件区拦（`<style>` 块/css 文件合法）；③死代码检测仅覆盖「空 catch」形态（AST 级 dead export 未做）。基线语义与 registry-gate 同款（存量豁免、只拦新增）。

## 台账定位

§4 强制清单第 5/6 条由 🔴（无专属门禁）升 ✅（regex 子集口径）；第 9 条（可达性 axe 子集）仍 🔴 诚实保留。
