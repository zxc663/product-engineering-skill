# 冷启动实测报告（2026-09-23 · 三条件 × 8 样本）

> 目的：回答「产品工程 Skill 落地可执行了吗」——拆成三问：①触发链通不通 ②跑道能不能被全新会话走通 ③发现断点后修复是否闭环。
> 方法：无头子代理=实验单元（每样本=独立 git 工作区+基线 commit+单一任务书）；三条件=**裸基线**（无注入）／**注入模拟**（提示词内联 v3.3.0 注入核心；N6b 起加平台式常驻 Skill 清单）／**显式点名**（明示加载 shisan-xinuo-product 并按跑道执行）；**判定=主会话实物检查**（产品工件 grep+门禁独立复跑），不以代理自述为准。
> 任务：待办清单（N1/N2/N3）与随手记账（N4/N5/N6/N6b/N6c）两种；每个样本独立工作区（`D:/产品工程冷启动-20260923*/`）。

## 一、结果矩阵

| 样本 | 条件 | 任务 | 触发产品包？ | 实物判定依据 |
|---|---|---|---|---|
| N1 | 裸 | 待办 | ✗ | 仅 index.html；产品工件 0 |
| N3 | 裸（N1 复刻） | 待办 | ✗ | app.js/index.html/tests；产品工件 0；无承载工件 |
| N4 | 裸 | 记账 | ✗ | 产品工件 0；**自述称「按注入纪律 L2-S 推进」但无任何承载工件——自述与实物背离实例** |
| N6 | 注入（无清单） | 记账 | ✗（路由无从发生） | **纪律层实物在**：memory/agent-log.md 四区+回指头、GATE 12 字段、errpath/caps 归因；无清单=模拟保真度缺口 |
| N6b | 注入+清单（修复前 description） | 记账 | ✗ | **能力检索确实执行且诚实归因**（caps 原话：「能力检索无直接命中…故内联」）；AGENTS.md 项目规则（含回指字段）+agent-log 在；产品工件 0 |
| N2 | 显式 | 待办 | ✓ | **八步全链 PASS**；主会话独立复跑：statechart-gate selftest／--file／--file --contract、spec-trace 全 rc=0 |
| N5 | 显式 | 记账 | ✓ | **全链 PASS＋正确自判档3**（删除/恢复触发线→档2 全量+recovery+evidence）；新 C1-C7（含 C7 对账）主会话独立复跑 rc=0；产品工件 6 文件命中 |
| N6c | 注入+清单（**修复后**） | 记账 | ✓ **命中即用** | `docs/product/` 四件工件在档；**层级门声明精确**（活动层=L5-L8，L0 由 brief 陈述、运营⑥显式裁决无后台）；账本 D1 按判定表判**档3**（依据=删除恢复线，被否候选=档1「拿不准按高一档」）；statechart+contract 主会话独立复跑 rc=0 |

**计数**：裸组 0/3 触发；显式组 2/2 全链通过；注入组路由 0/2（修复前）→ **1/1（修复后，N6c）**。

## 二、断点定位与修复（本实验最大产出）

1. **裸基线 0/3 触发** → 离开注入义务层，触发不可依赖——效力衰减链第 1 级（在场不触发=概率匹配非义务）的实验实证。
2. **注入+清单**：能力检索**执行了**、归因**诚实了**（#347 要求的行为全部到位），但判「无命中」→ **断点不在注入链路，在 description 触发面**：Use when 全是修复/审查场景词（难用/丑/反直觉/审设计稿），唯一建设侧措辞「前端/功能设计任务」权重不足，模型读到「做一个记账小工具」不认为命中。
3. **修复（第三次触发词密集化）**：Use when 增「**新增或修改用户可见功能面**（做一个XX页面/工具/应用｜新功能开发/功能落地均命中——触发后按 §7 档位自适配深度，小件走轻量档不重罚）」；已落源库+唯一安装根（`.agents`；`.zcode` 侧家族包已处置）。
4. **N6c=修复后复测**（同 N6b 条件，仅 description 更新）——闭环判定见下补记。

### 补记：N6c 复测结果（2026-09-23 · 修复闭环确认）

**同条件复测（仅 description 更新）：触发 ✓**。N6c 的 caps 字段自述「shisan-xinuo-product（命中即用）」，实物验证：`docs/product/` 四件（interaction-spec/statechart/contract/decision-ledger）+evidence 在档；层级门声明落笔（活动层 L5-L8、L0 陈述性检查、可运营性⑥显式裁决「无后台」）；账本 D1 按风险档位矩阵判档3 并写明被否候选；statechart-gate（C1-C7 含对账）独立复跑 rc=0；另自写 CDP 冒烟 25 断言全过、截图自查修掉一个折行缺陷。
**结论：断点定位正确、修复闭环**——「注入→能力检索→命中产品包→跑道」全链在模拟环境下走通。链上剩余的唯一未测环=真实会话（重启后）的 hooks 通道与平台原生清单渲染，属用户侧最终验收锚。

## 三、显式组=「跑道可执行」的端到端实证（两条任务全 PASS）

- **N2（待办·档3）**：0a 层级声明（正确应用「自答优先：项目工件无既有答案→0b 回补，不打断用户」）／0b 六问+盘点／8 态 statechart+契约／验收 GWT×5／证据 8 截图+3 DOM+可重跑 CDP 脚本／账本 D1-D8。**亮点**：走查实测揪出其自设计的死 UI（添加按钮「双防」屏蔽原生提交→行内提示永不可达），按「下游缺陷根因在上游→回改上游不就地打补丁」修掉——层级纪律的反向回补在真实缺陷上自发生。
- **N5（记账·档3）**：0a 声明主战场=L6/L7+L9/L10、上游由任务书给定（冷启动无工件→六问回补落档）；statechart+contract 跑 **C1-C7 含 recovery 双向对账**；证据 8 场景（初始/非法输入/添加/删除撤销/过期/存储错误/恢复）png+html+可重跑 e2e.mjs。
- **共同降级（如实）**：子代理无交互浏览器（`Browser is not available in subagent` 原话入档）→ Edge headless+CDP 真实内核取证替代；真人走查未做。

## 四、对产品的反哺

1. ✅ **description 构建侧触发词**（本实验直接产物，已落地）。
2. ✅ （既有纪律获得新证据）N4 的「自述遵守纪律但无承载工件」→ 支持 GATE 证据三挂靠与「实物优先于自述」，无需新机制。
3. 📋 （待用户侧）真实新会话最终验收锚：重启应用 → 新会话验「在场提示 · v3.3.0 + 373 条细则 + zxc663 应答」→ 说「做一个XX页面」看是否自动加载产品包（N6c 的真实版）。

## 五、边界（结论限定的条件）

- 子代理不继承平台注入全文与 hooks 全量通道；N6/N6b/N6c 为**提示词内联模拟**（无 hooks 推送）——是真实会话的下界模拟，最终判定以用户重启后的真会话为准。
- 每格 N=1（裸组 N=3）；模型随机性未消除；全部样本仅代理侧验证（headless Edge/CDP），无真人走查、无 Firefox/Safari。
- 判定工具=工件 grep+门禁独立复跑；「触发」的操作定义=产品工程产物在档（定义/statechart/门禁运行记录任一）。
