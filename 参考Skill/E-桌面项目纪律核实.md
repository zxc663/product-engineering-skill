# E 组核实 · 桌面各项目纪律与观点蒸馏（2026-09-21）

> 任务来源：用户「核实一下项目中的哪些纪律和观点值得一并拿出来，桌面还有其他项目」。
> 方法：桌面 11 目录盘点 → 只读纪律承载（AGENTS.md/知识沉淀/memory 机制），不碰数据与私人文件。

## 0. 盘点结论（11 目录 → 2 重点 + 1 复查 + 8 跳过）

- **重点核实**：`项目/`（多项目工作区：AGENTS.md 42 条+AI会话知识沉淀 40+ 条+踩坑经验库机制+4 子项目）、`博客项目/`（AGENTS.md=工作流 v3.0 项目化实例）。
- **复查**：`个人财务账单智能规划助手/`（方向档素材源，本轮确认无漏）。
- **跳过**：`1/`、`个人简历/`（私人文件）；`新建文件夹/`（素材包，内含**样本=真实账单，红线不碰**）；`财务助手-迁移包/`（安装包集合）；`个人财务账单智能规划助手-9.20/`（旧快照）；`小组/`（实训材料）；`tx-ob-test-vault/`、`新建文件夹`其余（空/测试）。

## 1. 「项目/」核实——价值最高，三项直接验证 + 一座素材矿

### 1.1 直接验证（用户观点早已实践固化）
1. **产品视角优先（AGENTS 第 13 条，2026-08-13 固化）**：「产品=体验与表现（用户表面可见的），功能=设计；**体验与表现优先于功能设计**；功能是否必要由产品角度判定」——比本 Skill 立项早一个月，与 9.71「30 万」类比完全同源。**验证性结论：联通层目的态不是新想法，是把已实践一个月的纪律机制化**。
2. **复用优先决策链（第 5 条）**：五问链=①功能必要性（**产品视角判定**）②系统原生 ③标准/组件库（shadcn 38 组件+Radix+ReactBits 二开）④现有依赖 ⑤最少代码；开源优先复用+二次开发风格适配；**自研必须记录调研结论与理由**（实例归因：插件系统参考 NocoBase、事件钩子参考 Typecho、邮件接收参考 Haraka）。**增量发现：该链第一问是产品视角判必要性，排在 ponytail 七级阶梯的「要不要存在」之前且更正确**——升格候选（见 §4）。
3. **验收标准前置（第 16 条）**：开工前 3-5 条可验证验收标准（Given/When/Then），先定标准再实现——兑现义务前移的又一实践。
4. **最小闭环交付（第 39 条）**：理解→最小修改→最小验证→直接交付，四步强制链（已入工作流，确认状态）。

### 1.2 机制同构先例（产品工程工件在「项目/」已有运行版本）
5. **踩坑经验库=能力地图的已运行形态**：开工强制必读+按症状关键词检索+四段式（症状→根因→解决→预防）+维护后更新时间戳（第 33/82 条）——**capability-map 的 harvest/检索/必读机制全部有先例**，产品工程能力地图=其泛化到 Skill 域。
6. **AI会话知识沉淀=判定表的本土素材库**：AI 版每条 1-3 句、**「触发场景｜判断｜行动」结构**（=judgement-table 行 schema）、双写个人版（类比+判断标准）、只追加+月度去重、头部索引最近 20 条（第 35 条）——**第一批判定表条目可直接从其 40+ 条实战条目选材**（出处=本土实战，说服力高于外部蒸馏）。
7. **规则六步优化流程（第 38 条）**：采集→五问→四段模板→用户审批→落盘复检→留档提交+`npm run rules:check` 自动检查——防膨胀三闸的实践版（规则膨胀/重复/断链有机器检查）。
8. **代码实况调研（第 42 条）**：开工先 rg 定位消费方/常量/开关，形成「现状证据」（文件+行号+结论）落盘；「不得用『我记得』『应该是』代替证据」——取证纪律实践版。
9. **分析即落盘（第 41 条）**：调研结论随产随存不等到收尾；压缩恢复后按留档续做不凭记忆——抗压缩实践。

### 1.3 实战判定素材（判定表候选，全部有实战语境）
ListRow+**5 秒撤销**（undo 安全窗）、SwitchRow **整行命中区**、Ctrl+S 保存、失败重试、`?tab=` 深链、脏点/字段数徽标、跨组搜索跳转高亮、对比度护栏、**「辉光变量无人写入」**（死配置=dead-binding 实例）、契约单测四态（data:null 合法成功/code 唯一成败依据）、aria-label 错位。

### 1.4 明确不拿
安全执行红线（越界零操作/绝对路径 rm/未跟踪文件授权单次有效/第三方安装体检）——**工作流域**（工作流 Skill 或平台层承载）；平台特定（MCP 直连通道/免费配额路由）；项目特有（双仓发布/测试基线数值/文档体系细节）。

## 2. 「博客项目/」核实——三条，两条已被工作流吸收
1. **漂移自检可重跑命令**：`diff -rq --exclude=... 双副本` 零输出=一致（自动清单替代人工记忆）——可重跑工件思想，已进工作流（确认）。
2. **「判级可以低，不可以隐」**：显式判级一行表述——已被 v3.2 硬注入吸收（Context 状态行）。
3. **接续豁免**：「跳过+声明合法，静默跳过违规」——已被吸收（细则 #326 邻域）。
4. memory 三文件（state/experience/preferences）+agent-log 入口档指针回指=承载收敛先例（一档制的变体，不另拿）。

## 3. 主财务项目复查——无漏，确认两件已进档
P1-P4 原型门禁（check-proto-parity 四断言可运行=产品工程三门禁的直接原型）、六态审查、实机走查取证、双轨数字红线（LLM 不碰数字）、L-002 环境统一——均已在方向档/上位调研档。本轮无新增。

## 4. 对 direction.md 的影响（三条升格 + 一处素材源登记）
1. **复用阶梯加「第 0 问」**：产品视角判定功能必要性排在「要不要存在」之前（用户实践 2026-08-13 固化，先于 ponytail 阶梯且更贴合联通层）——registry-gate/翻译表的第一道闸。
2. **验收标准前置并入联通工序**：翻译表产出后即写 3-5 条可验证验收标准（Given/When/Then），先于任何实现。
3. **能力地图机制对齐踩坑经验库先例**：开工必读+症状检索+四段式+维护时间戳+harvest——不发明新机制，泛化已验证机制。
4. **判定表首批 10 条选材源**：AI会话知识沉淀 40+ 条实战条目（触发场景｜判断｜行动现成格式）优先于外部蒸馏选材——本土实战出处说服力最高。

## 5. 工作流源库核实（D:/Agent工作流启动包/shisan-xinuo-workflow，git 仓双远端，B 组蒸馏只覆盖 skill 文本——本轮补仓库工程层）

1. **EVIDENCE.md with/without 对照路测（v1.8.0）**：双轨同指令同初始状态（without=通用子代理只读轨/with=按 11 步主流程落地）+9 维行为对照矩阵（判级/复述/验收前置/红线显式化/文档同步…）+局限诚实（N=1 样本声明）——**工作流已实践 agentic 基准，比 ponytail 更早；direction §四 的 agentic 对照验收直接套此格式**。
2. **RELEASE-CHECKLIST 发行工程化**：版本六处锁（package.json/SKILL frontmatter/README 徽章/项目信息/reference-sources/AGENTS）+verify-release 可复跑门禁（8 项）+facts_sync 机器事实断言+「**本清单=时点快照，每批次开工强制重写（防过期口径指挥发行）**」+push 单独批准+豁免面义务——Skill 作为产品的完整发行供应链，第四包挂靠后直接继承。
3. **三包家族分合范式**：skill/ 三包「独立可装、建议同装、按需加载、用完不驻留」——产品工程第四包挂靠的机制基础（本轮已按此实施钩子，源库本地 commit 34c4a08）。
4. **hooks 模板体系先例**：templates/hooks/session-start.example.sh+session-end.example.sh+hooks.example.json——生命周期钩子的形态先例（工作流回补四条之一有原型）。
5. **同源条款（引用不复制）**：CHANGELOG v3.2.0 细则 #371「GATE ev= 验证层级（exec/cover/invariant/indep）」与 #373「机器事实优先于 LLM 判据+公开数字须随仓可复算+效果主张须外部盲评」——与产品工程取证纪律同源，判表引用端口。
6. **明确不拿**：roadtest/scorecard/判据体系（j1.0-j2.5）=工作流自有效果验证基建——工作流域，引用端口不搬内容。
7. **核实过程实测**：promote-skill 在启动包根不在源库内（路径直觉错一次）；CHANGELOG 为简史制（每版一行关键意图，完整逻辑在 git log）——第四包 CHANGELOG 沿用此制。
